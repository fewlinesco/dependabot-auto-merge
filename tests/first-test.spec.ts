import { describe, expect, test } from "vitest";

describe("Fake test until we have real ones", () => {
  test("Goes green", () => {
    expect("Hello".split("")).toEqual(["H", "e", "l", "l", "o"]);
  });
});
