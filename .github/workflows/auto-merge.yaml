on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    name: AutoMerge test
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3
      - name: Auto merge
        uses: ./
        id: automerge
        with:
          gh-pat: ${{ secrets.GH_TOKEN }}
